<!-- 
    Copyright (c) 2012 Microsoft Corporation.  All rights reserved.
    Use of this sample source code is subject to the terms of the Microsoft license 
    agreement under which you licensed this sample source code and is provided AS-IS.
    If you did not accept the terms of the license agreement, you are not authorized 
    to use this sample source code.  For the terms of the license, please see the 
    license agreement between you and Microsoft.
    
    To see all Code Samples for Windows Phone, visit http://go.microsoft.com/fwlink/?LinkID=219604
-->
<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:local="clr-namespace:Microsoft.Phone.Controls;assembly=4charm">

    <Style TargetType="local:MediaViewer">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:MediaViewer">
                    <Canvas x:Name="LayoutRoot" Background="Transparent">
                        <Canvas 
              x:Name="MediaStrip" 
              Background="Transparent"
              VerticalAlignment="Top" 
              RenderTransformOrigin="0.5,0.5">
                            <Canvas.RenderTransform>
                                <CompositeTransform x:Name="MediaStripCompositeTransform"/>
                            </Canvas.RenderTransform>
                        </Canvas>
                    </Canvas>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="ItemTemplate">
            <Setter.Value>
                <DataTemplate>
                    <Grid Margin="0">
                        <ViewportControl x:Name="Viewport">
                            <Canvas x:Name="Canvas">
                                <local:ThumbnailedImageViewer 
                  x:Name="ZoomableContent"
                  CacheMode="BitmapCache"
                  RenderTransformOrigin="0,0">
                                    <local:ThumbnailedImageViewer.RenderTransform>
                                        <ScaleTransform x:Name="ZoomableContentTransform"/>
                                    </local:ThumbnailedImageViewer.RenderTransform>
                                </local:ThumbnailedImageViewer>
                            </Canvas>
                        </ViewportControl>
                        <ProgressBar x:Name="Progress" Value="50" Margin="0" Padding="0" VerticalAlignment="Top" HorizontalAlignment="Stretch"/>
                    </Grid>
                </DataTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="local:ThumbnailedImageViewer">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:ThumbnailedImageViewer">
                    <Grid>
                        <Grid.RenderTransform>
                            <CompositeTransform x:Name="GridTransform" />
                        </Grid.RenderTransform>

                        <local:GIFViewer x:Name="GIFImage"/>

                        <Image 
              x:Name="Image" 
              Stretch="Uniform" 
              HorizontalAlignment="Center"/>

                        <Rectangle 
              x:Name="Placeholder" 
              Width="480" 
              Height="360" />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="local:GIFViewer">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:GIFViewer">
                    <Grid>
                        <phone:WebBrowser x:Name="Browser"
                                          IsGeolocationEnabled="False"
                                          IsScriptEnabled="True"
                                          ScrollViewer.VerticalScrollBarVisibility="Disabled"
                                          IsHitTestVisible="False"
                                          VerticalAlignment="Center"
                                          HorizontalAlignment="Center"
                                          Width="0"
                                          Height="0"
                                          Visibility="Collapsed">
                        </phone:WebBrowser>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
